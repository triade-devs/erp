# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=          # Apenas server — NUNCA expor no client

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Serviços de enriquecimento (CEP, CNPJ, NCM, código de barras)
# URL base do enrichment-services. Em dev local, aponte para a porta dele (3001).
# Opcional: se ausente, o autocomplete é desativado e os campos são preenchidos à mão.
ENRICHMENT_URL=http://localhost:3001

