# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=          # Apenas server — NUNCA expor no client

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Feature flags
MULTITENANCY_ENABLED=false              # Apenas server — controla migração multi-tenant
