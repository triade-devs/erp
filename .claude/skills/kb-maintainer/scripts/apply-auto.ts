#!/usr/bin/env tsx
/**
 * apply-auto — aplica fixes mecânicos (severidade AUTO).
 *
 * TODO(M3): ler report.json, iterar items com severity=AUTO e aplicar
 * o handler correspondente. Cada handler é uma função pura `(item) => Patch`
 * que devolve mudanças a aplicar via lib/git.ts.
 */
console.log("[apply-auto] ainda em M0 — implementação real entra na fase M3.");
